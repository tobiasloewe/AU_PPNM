 \
	set terminal svg; \
	set title "Convergence of rmax"; \
	set output "plot.conv1.svg"; \
	set key bottom ;\
	set xlabel "rmax" ;\
	set ylabel "lowest energy" ;\
	plot  \
		"convdata.txt" index 0 using 1:3 with lines title "H atom EVD with cyclic Jacobi" \

